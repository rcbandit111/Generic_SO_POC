server:
  port: 8080
  servlet:
    context-path: /api
logging:
  level:
    com:
      backend: DEBUG
    org:
      springframework: DEBUG
      springframework.web: DEBUG
spring:
  application:
    name: poc
  config:
    activate:
      on-profile: dev
  jmx:
    enabled: false

